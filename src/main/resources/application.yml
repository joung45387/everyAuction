spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/everyAuction?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
    username: test
    password: 123456
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
server:
  servlet:
    session:
      tracking-modes: cookie
  port: 80